name: Pushするみたいな
on: push

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "hello world" > hello.txt
      - run: ls
      - run: git push origin
